# Local secrets and machine-specific paths — keep example file in repo
ha-sync.config.json

# Optional local registry snapshots (if you create this folder manually)
.ha-assistant-context/

# Leftover local Docker / HA data (no longer tracked in repo; safe to ignore)
dev/ha-config/
